description: Request User body
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        name:
          type: string
    example:
        name: "NewUserName"